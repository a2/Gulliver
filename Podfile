platform :ios, '8.0'
link_with 'Gulliver', 'Gulliver Tests'

use_frameworks!

pod 'Lustre', '~> 0.6.1'

target 'Gulliver Tests', exclusive: false do
  pod 'Nimble', '~> 0.4.2'
  pod 'Quick', '~> 0.3.1'
end
